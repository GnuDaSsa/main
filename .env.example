## MongoDB (required for pages that persist data)
# Example (Atlas):
# MONGODB_URI="mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority"
MONGODB_URI=""

## Optional defaults used by some pages
MONGODB_DB="automation_db"
MONGODB_COLLECTION="inspection_records"

## Tips page (page8.py) overrides collection key
TIPS_COLLECTION="tips"
